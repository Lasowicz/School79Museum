const artifactsImg = document.querySelectorAll('.artifacts_image');

artifactsImg.forEach(el => {
    el.addEventListener('mouseover', (event) => {

    })
})
artifactsImg.forEach(el => {
    el.addEventListener('mouseout', (event) => {

    })
})

